import React from "react";
import { Bar } from "react-chartjs-2";

function BarCharts({ sectorsData }) {
  return (
    <div className="content-page-con">
      kkk lIf you want to prevent the text from wrapping, you can apply
      white-space: nowrap; Notice in HTML code example at the top of this
      article, there are actually two line breaks, one before the line of text
      and one after, which allow the text to be on its own line (in the code).
    </div>
  );
}

export default BarCharts;
